
/*
class Query{
  val c = new Country

  def read_input() = {
    println("Please enter a Country Name or a Country Code")
    val in = scala.io.StdIn.readLine()
    in
  }
  def is_code_country(code: String): Boolean = {
    val code_ref = c.get_code()
    code_ref.foreach(i => {
      if (i.equals(code))
        true
    })
    false
  }

  def is_name_country(name: String): Boolean = {
    val name_ref = c.get_name()
    name_ref.foreach(i => {
      if (i.equals(name))
        true
    })
    false
  }

  def display_airport_runways(): Unit = {

  }

}*/